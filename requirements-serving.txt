# 공유 서빙 이미지 의존성 (Tfidf/Bert 함수 공용)
# torch는 CPU 전용 휠로 Dockerfile에서 별도 설치. 여기엔 sentence-transformers만 추가.
# TF-IDF 함수는 torch/sentence-transformers를 런타임에 로드하지 않음(lazy import).
numpy>=2.3.3
scipy>=1.13
scikit-learn>=1.7.2
fastapi>=0.115.0
mangum>=0.17.0
pydantic>=2.0
requests>=2.31
python-dotenv>=1.0.0
boto3>=1.34.0
sentence-transformers>=3.0.0
